<!DOCTYPE html>
<html lang="en">
	<%- include('./partials/head')%>
<body>
    <%- include('./partials/header')%>

    <main>
        <div class="post-detail container">
            <section class="center main-content post">
                <div class="info-usuario">
                    <img src="<%= posteo.usuario.foto %>" alt=""> <!--IMAGEN USUARIO POST-->
                    <p><a href="/users/detalleUsuario/<%= posteo.usuario.dni %>"><%= posteo.usuario.username %></a></p>
                </div>
                <img class="photo" src="<%= posteo.foto %>" alt=""> <!--IMAGEN POST-->
            </section>
            <section class="right">
                <h4>Comentarios</h4>
                <section class="comments">
                    <form action="" class="comment-form">
                        <input type="text" class="input" placeholder="Agregar comentario">
                        <button type="submit">Comentar</button>
                    </form>
                    <div>
                        <% for (let i = 0; i < posteo.comentarios.length; i++) { %>
                            <article class="comment info-usuario">
                                <img  src="<%= posteo.comentarios[i].usuario.foto %> " alt="">
                                <section class="content">
                                    <a class="user" href="/users/detalleUsuario/<%= posteo.comentarios[i].dni %>"><%= posteo.comentarios[i].usuario.username %></a>
                                    <span class="text"><%= posteo.comentarios[i].comentario %></span>
                                </section>
                            </article>
                        <% } %>
                    </div>
                </section>
            </section>
        </div>
    </main>
    <%- include('./partials/footer')%>

</html>