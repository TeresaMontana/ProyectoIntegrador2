<!DOCTYPE html>
<html lang="en">

	<%- include('./partials/head')%>

<body>
    <% if (locals.user != undefined) { %>
        <%- include('./partials/headerLogueado')%>
        <% } else { %> 
            <%- include('./partials/header')%>
            <% } %>
    <main>
        <section class="center main-content">
            <form class="form" action="/users/register" method="POST" enctype="multipart/form-data">
                <div class="title">Registrate</div>

            <% if (locals.errors) { %>
                <p class="error"  style="color:red"><%= locals.errors.message %> </p>
              <% } %>

            <div>
                <label for="usuario"> Nombre de Usuario</label>
                <input type="text" name="usuario" id="usuario" required/>
            </div>
            <div>
                <label for="email"> Email</label>
                <input type="email" name="email" placeholder="Inserte su email" required />
            </div>
            <div>
                <label for="password"> Contraseña</label>
                <input type="password" name="password" required>
            </div>
            <div>
                <label for="FotodePerfil"> Foto de perfil</label> 
                <input type="file" name="FotodePerfil" placeholder="Inserte tu foto de perfil" required>
            </div>
            <div>
                <label for="fecha"> Fecha de nacimiento</label> 
                <input type="date" name="fecha" placeholder="Inserte su fecha de nacimiento" required>
            </div>
            <div>
                <label for="dni"> DNI</label> 
                <input type="number" name="dni" placeholder="Inserte su DNI" required>
            </div>
            <button type="submit"> Registrate </button>
        </form>
        </section>
    </main>

    <%- include('./partials/footer')%>

</body>

</html>